{
  "name": "rabbittalk-server",
  "version": "0.1.0",
  "author": "Andre Coelho",
  "main": "app.js",
  "scripts": {
    "server": "nodemon app.js",
    "client": "cd ../client && npm start",
    "client-install": "cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.0.1"
  },
  "devDependencies": {
    "concurrently": "^6.0.1",
    "nodemon": "^2.0.7"
  }
}
